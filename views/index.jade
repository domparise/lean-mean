extends layout

block content
	div(ng-controller="Ctrl",ng-init="vars= #{JSON.stringify(vars)}")
		h1= title
		p Welcome to the lean-mean web-dev stack
		button(ng-click="loadMore()") ajax
		p {{vars}}